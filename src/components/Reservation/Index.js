import styled from "styled-components";

export default function Reservation() {
    return(
        <Wrapper>
            <div>Reservation</div>
        </Wrapper>
    );
}

const Wrapper = styled.div``;